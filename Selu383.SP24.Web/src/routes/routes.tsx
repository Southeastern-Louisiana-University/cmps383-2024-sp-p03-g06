import { useEffect, useState } from "react";

// interface HotelDto {
//   name: string;
// }
export default function Reservations() {
  // const [hotels, setHotels] = useState<HotelDto[]>([]);

  // useEffect(() => {
  //   fetch("/api/hotels", {
  //     method: "get",
  //   })
  //   .then<HotelDto[]>((r) => r.json())
  //   .then((j) => {
  //     setHotels(j);
  //   });
  // }
  // )
  return (
    <>
      {/* (hotels.map(hotel)) => (
      <p>{hotels.Name}</p>
    ) */}
      <p>Hotel 1 should go here</p>
      <p>Hotel 2 should go here</p>
      <p>Hotel 3 should go here</p>
    </>
  );
}
